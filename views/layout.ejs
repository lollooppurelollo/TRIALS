<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TrialMatch</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />
    <!-- Stili personalizzati -->
    <link rel="stylesheet" href="/css/style.css" />
    <style>
        :root {
            --light-sage: #c1d5bb;
            --sage: #91ad86;
            --dark-sage: #5e7256;
            --off-white: #f5f5f5;
            --dark-gray: #333333;
        }

        .bg-light-sage { background-color: var(--light-sage); }
        .bg-sage { background-color: var(--sage); }
        .bg-dark-sage { background-color: var(--dark-sage); }
        .bg-off-white { background-color: var(--off-white); }
        .text-dark-gray { color: var(--dark-gray); }
        .text-sage { color: var(--sage); }
        .focus\:ring-light-sage:focus {
            --tw-ring-color: var(--light-sage);
        }

        .peer-checked\:bg-sage:checked {
            background-color: var(--sage);
        }
    </style>
</head>
<body class="bg-off-white font-sans text-dark-gray">
    <header class="bg-white shadow-md">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-dark-sage">TrialMatch</a>
            <nav class="space-x-4">
                <a href="/" class="text-gray-600 hover:text-dark-gray transition-colors">Paziente</a>
                <a href="/trials" class="text-gray-600 hover:text-dark-gray transition-colors">Trial</a>
            </nav>
        </div>
    </header>

    <main class="container mx-auto px-6 py-8">
        <%- body %>
    </main>

    <script src="/js/client.js"></script>
</body>
</html>

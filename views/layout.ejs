<!-- views/layout.ejs -->
<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trials App</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
</head>
<body class="bg-light-gray text-dark-text font-inter">

    <main class="container mx-auto p-4 max-w-xl">
        <%- body %>
    </main>

    <nav class="fixed bottom-0 left-0 right-0 bg-white shadow-lg p-4 flex justify-around border-t border-gray-200">
        <a href="/patient" class="nav-button <% if (page === 'patient') { %>active<% } %>">
            <i class="fas fa-user-injured text-2xl"></i>
            <span class="text-xs mt-1">Paziente</span>
        </a>
        <a href="/trial" class="nav-button <% if (page === 'trial') { %>active<% } %>">
            <i class="fas fa-flask text-2xl"></i>
            <span class="text-xs mt-1">Trial</span>
        </a>
    </nav>

    <script src="/js/client.js"></script>
</body>
</html>


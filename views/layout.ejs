<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clinical Trials</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
            color: #4a5568;
            padding-bottom: 72px; /* Spazio per la navbar inferiore */
        }
        .container {
            max-width: 1200px;
        }
        :root {
            --color-sage: #9baf93; /* Verde salvia */
            --color-light-sage: #c7d2c1; /* Verde salvia chiaro */
            --color-dark-sage: #6c7e63; /* Verde salvia scuro */
            --color-dark-gray: #4a5568; /* Grigio scuro */
        }
        .bg-sage { background-color: var(--color-sage); }
        .bg-light-sage { background-color: var(--color-light-sage); }
        .text-sage { color: var(--color-sage); }
        .text-dark-sage { color: var(--color-dark-sage); }
        .text-dark-gray { color: var(--color-dark-gray); }
        .hover\:bg-dark-sage:hover { background-color: var(--color-dark-sage); }
        .focus\:ring-sage:focus { border-color: var(--color-sage); }
        .focus\:ring-light-sage:focus { border-color: var(--color-light-sage); }
        .hover\:shadow-lg:hover { box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05); }
    </style>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <main class="container mx-auto px-4 sm:px-6 py-8">
        <%- body %>
    </main>

    <nav class="fixed bottom-0 left-0 right-0 bg-white shadow-lg z-50">
        <div class="container mx-auto flex justify-around items-center h-16">
            <a href="/" class="flex-1 text-center py-2 transition-colors hover:bg-gray-100">
                <i class="fas fa-user text-xl mb-1 text-dark-sage"></i>
                <span class="block text-sm font-medium text-dark-sage">Paziente</span>
            </a>
            <a href="/trials" class="flex-1 text-center py-2 transition-colors hover:bg-gray-100">
                <i class="fas fa-stethoscope text-xl mb-1 text-dark-sage"></i>
                <span class="block text-sm font-medium text-dark-sage">Trial</span>
            </a>
        </div>
    </nav>

    <script src="/js/client.js"></script>
</body>
</html>